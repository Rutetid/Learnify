

import React from 'react';

function Card() {
    return (
        <div className="flex justify-center items-center min-h-screen relative"> {/* Add relative positioning */}
            <div className="bg-indigo-400 text-lg text-white font-sans rounded-xl text-left w-72 h-80 mx-7 my-5 p-4 relative"> {/* Add relative positioning */}
                <div className="absolute top-5 left-2 m-2 bg-gray-300 rounded-full w-8 h-8 flex items-center justify-center"> {/* Adjust the position */}
                    <span className="text-black font-bold">M</span>
                </div>
                <h1 className="mt-16 text-black ">Coding</h1> {/* Adjust the margin */}
                <h1 className=" text-black font-bold">Front End</h1>
					<h1 className="text-black font-bold border-b-2 border-black pb-16">Web Development</h1> {/* Add a line below */}
				<button className='bg-black  font bold py-2 px-4 rounded w-full my-5'>Start</button>
            </div>
        </div>
    );
}

export default Card;